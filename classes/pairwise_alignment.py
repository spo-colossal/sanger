

class PairwiseAlignment: 

    def __init__(self, query, reference, targets, cutsite): 
        self.query = query
        self.ref = reference 
        self.targets = targets
        self.cutsite = cutsite